const SCREEN = {
  HOME: 'home',
  ANIMATION: 'animation',
};

const OVERLAY = {
  TOP_FLASH_MESSAGES: 'overlay.top-flash-messages',
};

const MODAL = {};

const HIDDEN_OVERLAYS = Object.values(OVERLAY);
const HIDDEN_MODALS = Object.values(MODAL);

export { SCREEN, OVERLAY, MODAL, HIDDEN_OVERLAYS, HIDDEN_MODALS };
