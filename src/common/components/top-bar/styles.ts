import { hp } from '@lomray/react-native-layout-helper';
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  /**
   * Styles for the inner wrapper component.
   */
  inner: {
    paddingTop: hp(7.389), // 60
  },
});

export default styles;
